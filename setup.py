from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='It's a project to identify whether the sensors is working properly or not.(meaning that the sensor is giving errors or wrong data or not).',
    author='c17hawke',
    license='MIT',
)
